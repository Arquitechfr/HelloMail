{
  "name": "hellomail",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "pnpm --filter backend dev",
    "dev:all": "concurrently -n api,worker,web -c blue,magenta,green \"pnpm --filter backend dev\" \"pnpm --filter backend dev:worker\" \"pnpm --filter frontend dev\"",
    "dev:backend": "concurrently -n api,worker -c blue,magenta \"pnpm --filter backend dev\" \"pnpm --filter backend dev:worker\"",
    "build": "pnpm --filter backend build",
    "typecheck": "pnpm --filter backend typecheck",
    "start": "pnpm --filter backend start",
    "deploy": "node scripts/deploy-backend.mjs && node scripts/deploy-frontend.mjs",
    "deploy:backend": "node scripts/deploy-backend.mjs",
    "deploy:frontend": "node scripts/deploy-frontend.mjs"
  },
  "engines": {
    "node": ">=20.0.0"
  },
  "devDependencies": {
    "concurrently": "^10.0.5"
  }
}
